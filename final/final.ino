// Маркеры памяти
// void   - пустота     - космос      - ничего          - не использует память
// int    - integer     - целое число - память для целых чисел типа 12345
// char   - символ      - буква       - один символ ASCII
// long   - длинное     - большое целое число
// float  - дробное     - вещественное число с плавающей точкой
// double - двойная точность - большее дробное число с плавающей точкой
// bool   - булево значение - true/false (истина/лжи)

// Названия функций
// () — круглые скобки (Shift + 9, Shift + 0), обозначают аргументы функции
// {} — фигурные скобки (Shift + Х, Shift + Ъ), определяют начало и конец блока

// Сложные команды и конструкции


// Подключение библиотеки часов реального времени DS3231
#include <iarduino_RTC_time.h>           // правильно подключается библиотека
iarduino_RTC_time RTC_DS3231;            // объект класса часов реального времени

// Узнать текущее время
void vrem(){
    if (millis() % 1000 == 0) {          // каждые 1 секунду
        Serial.println(RTC_DS3231.getTime("d-m-Y, H:i:s, D"));  // получаем и выводим время
        delay(1);                         // задержка на 1 миллисекунду
    }
}

// Прочитать расписание
void schetRasp(){
    
}

// Вывести на экран
void ecran(){
   
}

// Проверить нажатие кнопки
void knop(){
   
}

void setup() {                            // слеш (/) — комментарий строки
    delay(300);                           // задержка на 300 мс перед началом
    Serial.begin(9600);                   // инициализация последовательного порта
    RTC_DS3231.begin();                  // запуск часов реального времени
}

void loop() {
    vrem();                               // проверка текущего времени каждую секунду
}